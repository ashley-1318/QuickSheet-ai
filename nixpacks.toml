[phases.setup]
nixPkgs = ["python311", "nodejs", "poppler_utils", "tesseract"]

[phases.install]
cmds = [
    "npm ci",
    "python3 -m ensurepip --upgrade",
    "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
